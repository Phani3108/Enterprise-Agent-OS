# Patterns to ignore when packaging Helm charts
.git/
.gitignore
.helmignore
*.md
ci.Jenkinsfile
dist/
rendered-manifests*.yaml
*.tgz
# Exclude local-only templates and values from production chart packages
*-local.yaml
