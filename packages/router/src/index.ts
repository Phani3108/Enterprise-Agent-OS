/**
 * @agentos/router — Package entrypoint
 */
export { ModelRouter } from './router.js';
export type {
    ModelProfile, RoutingRequest, RoutingDecision,
    RoutingStrategy, RouterConfig,
} from './router.js';
